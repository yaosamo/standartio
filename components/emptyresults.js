import styles from "../styles/Home.module.css";

export default function EmptyResults() {
  return (
    <p>
      No icons found, maybe try other search or want to submit request for an
      icon just clikc here{" "}
    </p>
  );
}
